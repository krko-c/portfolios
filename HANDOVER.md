# 포트폴리오 분석 앱 — 인수인계 문서

> 새 대화를 시작할 때 이 문서를 먼저 읽어주세요.
> 지금까지의 목적·작업 방식·판단 근거·현재 상태가 모두 담겨 있습니다.

---

## 1. 이 프로젝트가 무엇인가

**Streamlit으로 만든 단일 파일 포트폴리오 분석 앱**입니다.

- 파일: `app.py` (약 9,400줄) + `requirements.txt`
- 배포: Streamlit Cloud (`portfolios-stg.streamlit.app`), GitHub에 파일을 덮어써서 배포
- 데이터: yfinance (주가), FRED (거시지표, `pandas-datareader` 경유)
- 언어: 화면·주석 모두 한국어
- 사용자: 자산관리 실무 관점에서 쓰는 개인. 한국·미국 자산을 섞어서 봄

**교육·참고용 도구이며 투자 자문이 아닙니다.** 모든 화면 하단에 이 문구가 있습니다.

---

## 2. 작업 방식 (반드시 지켜주세요)

### 2-1. 파일 전달
- 작업 후 항상 `/mnt/user-data/outputs/app.py`로 복사하고 `present_files`로 전달
- `requirements.txt`가 바뀐 경우에만 함께 전달하고, **그 사실을 명시**
- 사용자는 GitHub에 파일을 덮어쓰는 방식으로 배포

### 2-2. 검증 루틴 (매번)
수정 후 반드시 실행:

```bash
python3 -m py_compile app.py
python3 -m pyflakes app.py
```

그리고 아래 **자동 검사 7종**을 돌립니다 (AST 기반, 오탐 없음):

| 검사 | 내용 |
|---|---|
| 함수 중복 | 같은 이름의 def가 둘 이상 |
| 위젯 key 중복 | `key="..."` 중복 |
| 모듈명 가림 | `go = ...` 처럼 모듈명을 변수로 덮어씀 |
| key 없는 위젯 라벨 중복 | key 없이 같은 라벨을 쓴 위젯 |
| 고아 들여쓰기 | 함수 밖에 들여쓰기된 코드 (AST로 판정) |
| 20줄 블록 완전중복 | 복사 사고 감지 (겹치지 않는 것만) |
| 신규 기능 존재 | 이번에 넣은 키워드가 실제로 있는지 |

**과거에 실제로 겪은 사고들이라 검사가 이렇게 늘어났습니다.**
- `go = st.button(...)`으로 plotly 모듈을 가려 앱이 죽음
- 같은 블록이 두 번 삽입되고 들여쓰기가 깨져 다른 화면에 섞임
- 되돌리다 함수 선언부가 잘림
- 정규식의 `\b`가 실제 백스페이스 문자로 저장돼 검출이 죽음

### 2-3. 계산 검증
새 계산 로직을 넣을 때는 **반드시 별도 스크립트로 먼저 검증**합니다.

- 정답을 아는 합성 데이터로 확인
- 실제 사례(2008·2020·2022 위기 등)와 대조
- 경계 상황(1종목, 변동성 0, 데이터 20일 등) 확인

앱 안에도 **자체 점검 12항목**이 있습니다 (`run_self_tests`, 도움말 → 🧪 자체 점검).
수정 후 이것도 돌려서 회귀가 없는지 확인하세요.

### 2-4. 응답 스타일
- **한국어**로 답변
- 결론부터, 불필요한 서론 없이
- 표와 짧은 문단 위주. 과한 이모지·불릿 남발 금지
- 코드 블록은 꼭 필요할 때만
- **자신이 한 실수를 감추지 말고 명확히 인정**할 것
- 사용자가 "대답만" 하라고 하면 코드를 고치지 말 것

---

## 3. 이 도구를 관통하는 원칙

수정할 때 이 원칙들을 어기지 마세요. 여러 번의 피드백으로 자리 잡은 것들입니다.

### 3-1. 과최적화·데이터 스누핑 경계
- 최적화는 **학습/검증 분리**가 기본. 표본외 성과를 먼저 보여줌
- 조합 탐색은 **순위를 학습 구간으로만** 매김 (표본외로 매기면 검증이 무의미)
- **1등만 강조하지 않고** 상위 조합 전반과 채택 빈도를 함께 봄
- 여러 기준일 반복 검증으로 우연을 걸러냄

### 3-2. 없는 정밀도를 만들지 않기
- 확률 대신 **경계선 거리**·점수로 불확실성 표현
- 표본이 적으면 **"통계적 의미 없음"** 을 명시 (에피소드 5회 미만 등)
- 설명력이 낮으면 **판정 유보** (R² 0.10 미만)
- 근거가 약한 값은 조용히 쓰지 말고 **경고와 함께**

### 3-3. 이어붙이면 안 되는 것
- **흩어진 국면 조각을 이어붙인 누적수익률·MDD는 쓰지 않음**
  (실제로 겪을 수 있는 경험이 아님)
- 대신 **에피소드별 통계**(중앙값·최악·최선)를 씀
- 연속 구간 낙폭은 스트레스 테스트 화면에서만

### 3-4. 화면과 계산의 일치
- 입력받은 설정은 **반드시 계산에 반영**하거나, 안 되면 명시
- 화면 문구가 실제 계산과 다르면 안 됨
- 각 결과 화면 하단에 **📋 이 결과의 가정** 패널

### 3-5. 미래 정보 차단
- 환율은 `ffill()`만 (bfill은 미래 환율이 과거로 들어감)
- FRED 거시지표는 **발표 지연 2개월** 반영
- 단, **사후 수정 효과는 제거 못 함**을 명시 (ALFRED 빈티지 필요)
- 거시 국면은 **현재 판단 전용**, 과거 백테스트에 쓰지 않음

---

## 4. 현재 화면 구성

```
분석
  📊 포트폴리오 분석        여러 구성 비교, 성과·낙폭·기여도·적립식
  📉 시장·거시 국면         [탭] 가격 기반 강세/약세  |  FRED 경기·물가 4분면
  🔗 자산 상관관계          행렬·위험군 클러스터·기간별·국면별·롤링
  🌩 스트레스 테스트        [탭] 과거 위기 재현  |  가상 충격 시나리오
배분
  🎯 포트폴리오 최적화      8가지 위험측도, 표본외 검증, 워크포워드, HRP
  🧭 뷰 기반 자산배분       [모드] 시장·거시 전망  |  자산수익률 전망(BL)
  ➕ 자산 추가 효과         [모드] 고정 비중 편입  |  최적 조합 탐색
결정
  ⚖️ 최종 대안 비교         여러 안을 같은 조건으로 비교
  📖 도움말                사용법·지표사전(29개)·방법론(10장)·자체점검
```

### 주요 함수 위치 (줄 번호는 대략)

| 함수 | 역할 |
|---|---|
| `render_ticker_table` | **모든 화면 공통 종목 입력 표.** 복사/붙여넣기·ETF 불러오기·티커 확인 포함 |
| `assumptions_panel` | 결과 화면 하단 가정 요약 |
| `run_self_tests` | 자체 점검 12항목 |
| `validate_setup` / `show_msgs` | 공통 입력 검증 |
| `_opt_solve` | 최적화 solver. **종목별 배열 제약** 지원 |
| `black_litterman` / `bl_weights` | BL 계산 |
| `shock_asset` / `combine_shock` | 가상 충격 (자산 유형별) |
| `factor_betas` / `standardize` | 요인 민감도 (다중회귀 + 표준화) |
| `classify_regime` | 가격 기반 국면 (−20% 규칙) |
| `build_macro_regime` | FRED 경기·물가 4분면 |

---

## 5. 반복해서 지적받은 것들 (같은 실수 금지)

### 5-1. Streamlit 특성
- **버튼은 누른 그 실행에서만 True.** 아래 위젯을 건드리면 결과가 사라짐
  → `session_state`에 결과 유지 플래그를 두고, **입력이 바뀔 때만 초기화**
- 위젯 생성 후 `session_state[key]` 대입 불가
  → **세대 카운터**로 위젯을 새로 만듦 (`gen_key`)
- `multiselect`에 `default=`와 세션 값을 함께 주면 세션 값이 무시됨
  → `setdefault` 후 `default=` 없이

### 5-2. 계산에서 자주 틀린 것
- **회전율**: 워크포워드에서 매 구간 100% 재구축으로 잡혀 비용 과다 차감
  → `prev_w`로 직전 비중을 넘겨 실제 교체분만
- **누적 초과수익**: 일별 차이를 복리로 굴리면 실제와 벌어짐
  → 상대 부(富) 기준 `(1+rp).cumprod()/(1+rb).cumprod()-1`
- **환율**: 덧셈이 아니라 **곱셈** `(1+자산)(1+환율)-1`
- **볼록성**: 단위는 연²(TLT 약 250). 3.4 같은 값을 넣으면 무시됨
- **신용 스프레드**: 금리 듀레이션과 별도. 하이일드가 우량채보다 민감해야 함
- **0분모**: `std() == 0` 대신 임계값(`_EPS_STD`) 비교
- **요인 스케일**: 변동성이 다른 요인을 그대로 회귀하면 계수가 왜곡
  → 표준화 후 비교

### 5-3. 표현
- MDD는 음수 → `-20% → -15%`는 **개선**. 「개선(%p)」으로 표기
- "그 시점에 알 수 있었던 값"처럼 **과장하지 말 것**
- 상대평가면 상대평가라고 명시

---

## 6. 아직 안 한 것

### 6-1. 진행 중인 흐름 (시장·거시 전망 2단계)
1단계(전망 입력 + 영향 분석)까지 완료. **비중 조정은 아직 없음.**

2단계 설계 시 반영할 것:
- **√R² 가중**은 이미 1단계에 들어감. 틸트에도 적용해야 함
- **R² 0.10 미만 자산은 틸트 대상에서 제외**하거나 폭을 줄일 것
- 제약은 **최대 변경폭부터** 시작. 7개를 한꺼번에 걸면 해가 없어짐
- 적용 범위 3단계: 영향 분석만 / 보유자산 내 조정 / 후보자산 포함

**실측된 R² (참고)**

| 자산 | 3년 | 5년 |
|---|---|---|
| TLT | 0.89 | 0.86 |
| QQQ | 0.56 | 0.56 |
| SPY | 0.50 | 0.48 |
| EEM | 0.37 | 0.40 |
| GLD | 0.19 | 0.23 |
| SK하이닉스 | 0.12 | 0.11 |
| 삼성전자 | 0.05 | 0.07 |

→ 미국 자산은 안정적, **한국 개별종목은 이 요인 세트로 설명 안 됨**
(업황·메모리 가격·원달러·중국 경기가 지배. 거시와 무관하다는 뜻은 아님)

### 6-2. 미착수 목록

**기능 추가 (필요할 때)**
- 분석: 롤링 초과수익률, 적립식 별도 탭
- 최적화: 총 회전율 한도, 거래비용 페널티, 자산군별 한도, 추적오차 한도, 공분산 축소추정
- 뷰 기반: 확신도별 비교, 뷰 하나 제외 시 변화
- 상관관계: 하락월 상관, Spearman, PCA effective rank
- 스트레스: 위기 구간 상관 (일부는 이미 있음)

**신규 화면 (논의만 함)**
- 투자목적·제약 설정, 성과귀속, 상품전략, 유동성·실행가능성, 인덱스 개발, 의사결정 일지

**구조**
- **코드 분리** (9,400줄 → 여러 파일). **기능이 안정된 뒤에 하기로 함.**
  지금 하면 새 기능을 붙이며 다시 흐트러지고, GitHub 업로드 파일이 늘어남

---

## 7. Claude Code로 옮긴 뒤

### 7-1. 저장소 구조 (권장)

```
repo/
  app.py                 앱 본체
  requirements.txt
  HANDOVER.md            이 문서
  tools/
    check.py             정적 검사 7종
    selftest.py          계산 정합성 12항목
```

`tools/check.py` 와 `tools/selftest.py` 는 함께 전달됩니다.
저장소에 `tools/` 폴더를 만들어 넣으세요.

### 7-2. 기본 명령

```bash
# 실행
streamlit run app.py

# 검사 (수정할 때마다)
python3 -m py_compile app.py && python3 -m pyflakes app.py
python3 tools/check.py
python3 tools/selftest.py
```

### 7-3. 옮긴 직후 할 일 (순서 지킬 것)

1. `streamlit run app.py` 로 앱이 뜨는지 확인
2. **9개 화면을 하나씩 열어보기** — 지금까지 화면 렌더링 오류는
   배포 후에야 발견됐습니다. 로컬에서 먼저 잡을 수 있습니다
3. `tools/check.py`, `tools/selftest.py` 가 통과하는지
4. yfinance·FRED 실제 조회가 되는지 (개발 컨테이너에서는 막혀 있었음)

**여기까지 확인한 뒤에** 코드 분리나 새 기능으로 넘어가세요.
한 번에 여러 가지를 바꾸면 문제 원인을 못 찾습니다.

### 7-4. 코드 분리 (안정된 뒤에)

권장 구조:

```
app.py                  진입점·사이드바·라우팅만 (200줄 이하)
core/
  data.py               티커 정규화, 가격 로딩, 환율
  metrics.py            수익률·위험·비율 지표
  portfolio.py          비중·리밸런싱·회전율·기여도
  optimize.py           solver, 효율적 투자선, HRP
  models.py             블랙-리터만, 요인 회귀, 충격 모형
  regimes.py            가격 국면, FRED 거시 국면
pages_/
  analysis.py  optimizer.py  correlations.py  regimes.py
  stress.py    view_alloc.py add_effect.py    compare.py
  help.py
ui/
  tables.py             render_ticker_table 등 공통 위젯
  panels.py             assumptions_panel 등
```

**분리 순서** (한 번에 다 옮기지 말 것):

1. `core/` 부터 — 계산 함수는 Streamlit 의존이 없어 안전
   옮길 때마다 `tools/selftest.py` 로 확인
2. `ui/` — 공통 위젯
3. `pages_/` — 화면 하나씩. 옮길 때마다 실행해서 확인
4. 마지막에 `app.py` 정리

**주의**: 전역 설정(`base_ccy`, `cost_bp`, `fx_hedge`, `use_div`, `gap_fill`,
`rf_rate`)을 함수에서 그냥 참조하는 곳이 많습니다. 분리하면 전부 인자로
넘기거나 설정 객체로 묶어야 합니다. **이 과정에서 실수가 가장 많이 납니다.**

### 7-5. 커밋 단위

- 기능 하나 = 커밋 하나
- 검사 통과 후에만 커밋
- 메시지는 한국어로, 무엇을 왜 고쳤는지

---

## 8. 개발 환경 제약

- **yfinance·FRED 접속 불가** (개발 컨테이너에서 차단)
  → 계산 검증은 합성 데이터로, 실제 결과는 사용자가 캡처로 확인
- **Streamlit 실행 불가**
  → 화면 렌더링 오류는 못 잡음. 배포 후 사용자가 확인
- 사용 가능: `numpy`, `pandas`, `scipy`, `plotly`, `xlsxwriter`, `pyflakes`

---

## 9. 첫 응답 요령

새 대화 시작 시:

1. 이 문서를 읽었다고 짧게 확인
2. `/mnt/user-data/uploads/`에 `app.py`가 올라와 있으면 `/home/claude/v3/`로 복사
3. **바로 작업 시작.** 장황한 요약 금지

사용자가 GPT 피드백 캡처를 주면:
- **추측하지 말고 코드로 사실 확인 먼저**
- 반박할 게 있으면 근거와 함께 반박
- 우선순위를 나눠 정리 (즉시 수정 / 나중 / 반대)
